{
  "name": "f1be",
  "version": "1.0.0",
  "description": "f1 BE",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "exec": "node dist/server.js",
    "build-and-run": "npm run build && npm run exec"
  },
  "author": "fdc",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.8",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
